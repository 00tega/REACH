import React from 'react';

/**
 * RoleSwitcher has been removed per user design requirements.
 * Navigation is handled exclusively via the role-aware Sidebar,
 * with authentication controls at the bottom of the sidebar.
 */
export const RoleSwitcher: React.FC = () => null;
